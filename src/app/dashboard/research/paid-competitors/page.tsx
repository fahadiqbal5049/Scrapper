import React from "react";
import PaidCompetitors from "@/components/dashboard/tabs/research/paidCompetitors/PaidCompetitors";

const page = () => {
  return (
    <React.Fragment>
      <PaidCompetitors />
    </React.Fragment>
  );
};

export default page;
